# Read me for RDS Schema
postgressql
* `schema.sql` schema for RDS 
* `ERD.png` ERD diagram showing our tables
* `create_rds.py` python script that runs schema

## Requirements:
    pip install -r requirements.txt


## .env file format:
host=your-host
port=port
user=use
password=psw
dbname=your-dbname